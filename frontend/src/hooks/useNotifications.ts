// This file is now just a re-export to maintain backward compatibility
// and point to the new Context-based hook.
export { useNotifications, NotificationProvider } from '../context/NotificationContext';
